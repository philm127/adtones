
@{
    Layout = null;
}

<!DOCTYPE html>

<div class="container">

    @*<input type="text" id="TxtMessage" style="width:30%;"/>

    <input type="button" id="BtnSend" value="Send" />*@

    <input type="hidden" id="UserName" />

    <ul id="Chats"></ul>

</div>


<script src="~/Scripts/jquery-2.1.1.js"></script>
<script src="~/Scripts/jquery.signalR-1.1.4.js"></script>
<script src="~/signalr/Hubs"></script>

@*<script>

        $(function () {



            var chat = $.connection.chatHub;

            chat.client.NewMessage=function(Cl_Name, Cl_Message) {

                $('#Chats').append('<li><strong>' + htmlEncode(Cl_Name)

                    + '</strong>: ' + htmlEncode(Cl_Message) + '</li>');

            };

            $('#UserName').val(prompt('Please Enter Your Name:', ''));

            $('#TxtMessage').focus();

            $.connection.hub.start().done(function () {

                $('#BtnSend').click(function () {

                   // chat.server.LetsChat($('#UserName').val(), $('#TxtMessage').val());
                    chat.server.letsChat($('#UserName').val(), $('#TxtMessage').val());
                    $('#TxtMessage').val('').focus();

                });

            });

        });



        function htmlEncode(value) {

            var encodedValue = $('<div />').text(value).html();

            return encodedValue;

        }

</script>*@


<script>

        $(function () {

            var chat = $.connection.adDetailHub;


            chat.client.AdName = function (userAdName) {
                $('#Chats').append('<li><strong>' + userAdName + '</li>');
            };

            //$.connection.hub.start().done(function () {
              
            //    $('#BtnSend').click(function () {

            //        chat.server.getAdsName($('#TxtMessage').val());
            //        $('#TxtMessage').val('').focus();

            //    });
            //});
           
            
            //For direct call
            $.connection.hub.start().done(function ()   {               

                chat.server.getAdsName('@ViewBag.number');
                $('#TxtMessage').val('').focus();
               
            });

        });



        function htmlEncode(value) {

            var encodedValue = $('<div />').text(value).html();

            return encodedValue;

        }

</script>
