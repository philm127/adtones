@model EFMVC.Web.ViewModels.CampaignProfilePressFormModel
@{
    ViewBag.Title = "Press";
}
<div class="row">
    @using (Html.BeginForm("SavePress", "Campaign"))
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true)

        @Html.Partial("_CreateOrEditPress", Model)

        <p>
            <input type="submit" value="Submit" class="font12" />
        </p>
    }

    <div>
        @Html.ActionLink("Back to List", "Index")
    </div>
</div>