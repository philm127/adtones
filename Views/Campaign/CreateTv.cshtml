@model EFMVC.Web.ViewModels.CampaignProfileTvFormModel
@{
    ViewBag.Title = "TV";
}
<div class="row">
    @using (Html.BeginForm("SaveTv", "Campaign"))
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true)

        @Html.Partial("_CreateOrEditTv", Model)

        <p>
            <input type="submit" value="Submit" class="font12" />
        </p>
    }

    <div>
        @Html.ActionLink("Back to List", "Index")
    </div>
</div>